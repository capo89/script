using ScmGroup.XCam.MachiningDataModel;

namespace ScmGroup.XCam.Scripting;

public class EndPointAttribute : Attribute
{
	public EndPointAttribute()
	{
		_ = 8;
		if (4 == 0)
		{
		}
		base._002Ector();
	}

	public EndPointAttribute(OperationAttribute value, int index = 0)
	{
		_ = 8;
		if (false)
		{
		}
		_ = 5;
		if (1 == 0)
		{
		}
		_ = -1;
		if (6 == 0)
		{
		}
		base._002Ector(value, index);
	}

	public EndPointAttribute(EndPointAttribute attribute)
	{
		OperationAttribute value = ((attribute.Value != null) ? (attribute.Value.Clone() as OperationAttribute) : null);
		if (2u != 0)
		{
			base.Value = value;
		}
		int index = attribute.Index;
		if (true)
		{
			base.Index = index;
		}
	}
}
